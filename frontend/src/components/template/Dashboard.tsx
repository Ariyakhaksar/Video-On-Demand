import React from "react";
import TitleLabelPages from "../modules/dashboard/TitleLabelPages";

type Props = {};

const Dashboard = (props: Props) => {
   return (
      <section>
         <TitleLabelPages title="پیشخوان" />
      </section>
   );
};

export default Dashboard;
